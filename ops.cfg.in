# Cancer Data Expo — Operations
# =============================
#
# Deploys the Cancer Data Expo for operational use, nominally on
# cancer.jpl.nasa.gov.
#
# To use:
#
# 1. Copy this file to ops.cfg
# 2. Set the passwords!  *Very important!*
# 3. Bootsrap: python2.7 bootstrap.py -c ops.cfg
# 4. Buildout: bin/buildout -c ops.cfg
#
# Copyright 2012–2016 California Institute of Technology. ALL RIGHTS
# RESERVED. U.S. Government Sponsorship acknowledged.


# Supervisor
# ----------
#
# Please set a better password for the process Supervisor.
[supervisor]
username = 
password = 

# Zope
# ----
#
# Please set a better password for the Zope app server.
[zope]
username = 
password = 
# We need to explicitly set this in operations (CA-1411):
environment-vars =
    TZ UTC
    HOME /home/edrn


# User IDs
# --------
#
# Unix accounts under which to run services.  Supervisor runs as root.
[users]
zope = edrn


# Buildout
# --------
#
# Bring it all together.
[buildout]
extends = etc/base.cfg
update-versions-file = ${buildout:directory}/var/ops-versions.cfg
eggs-directory = /usr/local/edrn/buildout/eggs
download-cache = /usr/local/edrn/buildout/downloads
extends-cache = /usr/local/edrn/buildout/extends
parts =
    lxml
    zope
    zope-debug
    repozo
    zope-backup
    supervisor
    init-script
    logrotate
    update-rdf
    update-summary
